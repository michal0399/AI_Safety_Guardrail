repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-added-large-files
      - id: check-json
      - id: check-yaml
      - id: detect-private-key
      - id: end-of-file-fixer
      - id: trailing-whitespace

  - repo: https://github.com/psf/black
    rev: 24.1.1
    hooks:
      - id: black
        language_version: python3.13
        args: ["--line-length=120"]

  - repo: https://github.com/PyCQA/isort
    rev: 5.13.2
    hooks:
      - id: isort
        args: ["--profile", "black", "--line-length=120"]

  - repo: local
    hooks:
      - id: bandit
        name: bandit (security)
        entry: bandit
        args: ["-l", "-i", "--exclude", "tests/"]
        language: python
        language_version: python3.13
        additional_dependencies: ["bandit>=1.7"]
        types: [python]

      - id: pytest
        name: pytest (unit tests)
        entry: python tools/precommit_pytest.py
        language: system
        pass_filenames: false
        stages: [commit]
        types: [python]

      - id: safety
        name: safety (dependencies)
        entry: safety
        args: ["check", "--short-report"]
        language: python
        language_version: python3.13
        additional_dependencies: ["safety>=3.7.0"]
        pass_filenames: false
        stages: [commit]
