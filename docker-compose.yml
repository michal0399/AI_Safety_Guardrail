version: '3.8'

services:
  api:
    build: .
    container_name: safety_guardrail_api
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./src:/app/src
      - ./safety_rules.txt:/app/safety_rules.txt
    restart: unless-stopped
